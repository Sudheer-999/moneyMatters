import NavBar from "../NavBar";
import SideBar from "../SideBar";
import DashboardBody from "../DashboardBody";

const Home = () => {
  return (
    <div>
      <SideBar />
      <NavBar />
      <DashboardBody />
    </div>
  );
};

export default Home;
